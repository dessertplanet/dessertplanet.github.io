---
title: "{{ replace .File.ContentBaseName `-` ` ` | title }}"
weight: 99
summary: ""
tags: []
links:
  - name: "GitHub"
    url: ""
---

Write the longer version here.

<!--
Embeds available in this body:

  {{</* youtube id="VIDEO_ID" class="embed embed-video" */>}}
      Hugo's built-in. Pass the class: given one, it emits that wrapper instead
      of its own inline aspect-ratio box, which is what `.embed` spacing and the
      16:9 rule in style.css hook onto. It also takes start=, end=, title=,
      loop=, mute=, controls= and loading=.
  {{</* bandcamp album="123456789" */>}}
  {{</* bandcamp track="123456789" */>}}

For a WebMIDI/WebAudio widget, drop the module in static/js/ and add:

  <script type="module" src="/js/your-widget.js"></script>
-->
